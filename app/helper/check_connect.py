from loguru import logger

from app.dbs.mongo import Mongo


def count_connection():
    #     server_info = Mongo.client.server_info()
    #     connections = server_info.get("connections", {}).get("current", 0)
    #     logger.debug(f"Mongodb info: {connections}")
    #     logger.debug(f"Number of connection: {connections}")
    pass
